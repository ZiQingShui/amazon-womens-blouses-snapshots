# 共享文件夹部署说明（纯静态 · 局域网）

看板现在是纯静态站点，不再依赖线上后端。放在共享文件夹里，通过局域网 HTTP 访问即可。

## 一、启动服务（在共享机 192.168.16.75 上做一次）

1. 把整个项目文件夹（含 `docs/`、`start-server.bat`）放到共享目录。
2. 双击运行 `start-server.bat`。脚本会自动探测 Python 并启动静态服务器，监听 `0.0.0.0:8000`。
3. 保持这个黑色命令行窗口开着，服务就在运行。

## 二、其他人如何访问

在任意电脑浏览器打开：

```
http://192.168.16.75:8000/
```

（如果共享机 IP 变了，把地址里的 IP 换成共享机当前的内网 IP。）

## 三、注意事项

- **共享机要保持开机**，且 `start-server.bat` 的窗口不能关。
- 若端口 8000 被占用，编辑 `start-server.bat` 里的 `PORT=8000` 改成其他端口，访问地址也同步改。
- 首次打开如看到旧内容，按 `Ctrl + F5` 强制刷新。
- 数据更新方式：更新 `docs/data/` 下的 JSON 快照后，刷新浏览器即可（无需重启服务）。
- 类目新增/删除：本版本界面上的「新增类目」按钮需要后端，静态场景下不可用。请在共享机上用命令行：
  - 新增：`python tools/add_category.py`
  - 删除：编辑 `docs/data/categories.json`，移除对应类目后同步 `dist/data/categories.json` 并重建（详见 README）。

## 四、常见问题

**Q：双击 `start-server.bat` 窗口一闪而过，或报一堆「不是内部或外部命令」**

检查这个文件有没有被编辑器改过。它必须是 **Windows 换行（CRLF）+ GBK 编码**。用记事本打开另存时会默认存成 UTF-8，cmd 就读不懂了——现象就是命令被拆成碎片乱报错。

修复：重新拷一份原始的 `start-server.bat`（或用 Git Bash 跑 `file start-server.bat` 确认显示 `ASCII/ISO-8859 text, with CRLF line terminators`）。

**Q：直接从网络路径 `\\192.168.16.75\...\` 双击可以吗？**

可以。脚本内部用 `pushd` 进入自己所在目录，它会为 UNC 路径自动分配一个临时盘符。**不是必须先映射 Z 盘。**

（早期版本用的是 `cd /d`，那个不支持 UNC，会导致「UNC 路径不受支持」并落到 `C:\WINDOWS`，现已修正。）

**Q：提示「[错误] 这台电脑没有找到 Python」**

共享机上需要装 Python 3：官网 python.org/downloads，安装时**务必勾选 `Add Python to PATH`**，装完重新双击脚本。

**Q：双击后 cmd 先打印「UNC 路径不受支持。默认值设为 Windows 目录」，正常吗？**

**正常，忽略即可。** 这是 Windows 自己启动命令行时的提示（它不允许把网络路径设为工作目录），**不是脚本出错** —— 脚本内部用 `pushd` 自己处理了路径，所以后面照样能找到 `docs/` 并正常启动服务。只要最后出现 `Serving HTTP on 0.0.0.0 port 8000` 就是成功了。

嫌它碍眼的话：把共享盘映射成盘符（如 `Z:`），从 `Z:\BS&NR榜单\...` 双击就不会出现这句。

**Q：同事打不开 `http://192.168.16.75:8000/`（浏览器一直转圈 / 超时）**

最常见的原因是**共享机的防火墙没放行 8000 端口**。自查方法：如果你能打开 `\\192.168.16.75\大想组-共享盘`（Z 盘），说明网络本身是通的，那问题就在端口被拦。

放行方法 —— 在共享机上用**管理员身份**打开命令提示符，执行一次即可（永久生效）：

```
netsh advfirewall firewall add rule name="看板 8000" dir=in action=allow protocol=TCP localport=8000
```

或者走图形界面：控制面板 → Windows Defender 防火墙 → 高级设置 → 入站规则 → 新建规则 → 端口 → TCP `8000` → 允许连接 → 配置文件勾「专用」即可。

> 补充：首次启动服务时，Windows 通常会弹一个「安全警报 —— 是否允许 Python 在此网络上通信」。如果当时点了「取消」，结果和防火墙没放行一样，用上面这条命令可以补上授权。

其他要排查的：① 共享机上那个黑窗口是否还开着（关了服务就停了）；② 共享机是否休眠了；③ IP 是否变过（换成本机当前内网 IP，`ipconfig` 可查）。

**Q：共享机自己怎么确认服务是好的？**

在共享机本机上打开浏览器访问 `http://localhost:8000/`。本机访问不受防火墙影响，能打开就说明服务和文件都没问题，剩下的只是「别人连不连得进来」。
